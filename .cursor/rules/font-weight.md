# Font weight: max semi-bold

Use at most `font-semibold` (600) in UI components. Do not use `font-bold`, `font-extrabold`, or `font-black`.

When adding or editing styles, prefer `font-medium` or `font-semibold` for emphasis instead of bolder weights.
